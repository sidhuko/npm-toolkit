var fs = require('fs-extra');

module.exports = function () {
  // copy __dirname + '/ntrc-sample' to ntrc
  var ntInstalled = false;
  var ntrcExists = false;

  console.log('Created ntrc folder in your current dir');
}
